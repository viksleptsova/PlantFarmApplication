<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="5dp"
    android:layout_gravity="center">


    <ImageView
        android:id="@+id/plantsImageView"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_margin="10dp"
        android:src="@drawable/plants_events_rec"
        app:layout_constraintTop_toTopOf="parent"
        tools:layout_editor_absoluteX="10dp" />

    <TextView
        android:id="@+id/eventName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:textSize="20sp"
        android:fontFamily="@font/open_sans_condensed_bold"
        app:layout_constraintStart_toStartOf="@id/plantsImageView"
        app:layout_constraintTop_toTopOf="@id/plantsImageView"
        app:layout_constraintBottom_toBottomOf="@id/plantsImageView"
       />

    <TextView
        android:id="@+id/eventDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="15dp"
        android:textSize="12sp"
        android:fontFamily="@font/open_sans_condensed_bold"
        app:layout_constraintEnd_toEndOf="@id/plantsImageView"
        app:layout_constraintTop_toTopOf="@id/plantsImageView"
        app:layout_constraintBottom_toBottomOf="@id/plantsImageView"
        />


</androidx.constraintlayout.widget.ConstraintLayout>